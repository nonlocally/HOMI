.DS_Store
*.log
*.pyc
__pycache__/
# runtime state lives in $XDG_STATE_HOME/communicate, never in the repo — but
# guard against accidental local copies:
/state/
/.state/

# Capability cards describe real hosts/corpora — schema is shared, entries are not.
registry/*.md
!registry/README.md
!registry/EXAMPLE.md
